// object é uma função
console.log (typeof Object);

class Produto {};
// classe Produto (objeto), por debaixo dos anos, não é nada mais, nada menos, que uma função (apenas declarada de uma forma diferente) 
console.log(typeof Produto);